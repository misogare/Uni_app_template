<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidFundamentalsLab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- Set status bar background color -->
        <item name="android:windowLightStatusBar">true</item> <!-- Set status bar icons and text to be dark (black) -->
        <item name="android:windowBackground">@drawable/rounded_window</item>
    </style>
    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20%</item>
    </style>
    <style name="Theme.AndroidFundamentalsLab" parent="Base.Theme.AndroidFundamentalsLab" />
</resources>